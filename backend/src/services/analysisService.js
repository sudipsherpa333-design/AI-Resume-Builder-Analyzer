export async function generateResumeAnalysis(resume) {
    // Minimal stub for development: returns light analysis
    return {
        score: 70,
        strengths: ['Clear formatting', 'Good structure'],
        weaknesses: ['Few metrics', 'Add role-specific keywords']
    };
}
